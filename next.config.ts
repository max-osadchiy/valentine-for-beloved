const nextConfig = {
    output: "export",
    basePath: "/valentine-for-beloved",
    assetPrefix: "/valentine-for-beloved/",
    trailingSlash: true,
    images: { unoptimized: true },
    distDir: "out", // Изменяем папку билда
};

export default nextConfig;
